# ADR-0007: Explicit partial surface mode

Status: accepted, 2026-09-16.

## Context

DOM20 is much larger than DGM1.

## Decision

Critical candidate regions by default, optional complete ray sampling, visible incomplete label.

## Alternatives and consequences

Silently treating DGM maxima as guaranteed DOM maxima is scientifically invalid. State the partial scope instead.
